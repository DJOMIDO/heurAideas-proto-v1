# Force shell scripts to use LF line endings
*.sh text eol=lf

# Force Python files to use LF line endings
*.py text eol=lf

# Other text files are automatically handled, but submitted with LF line endings
* text=auto